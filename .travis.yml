language: python
python:
  - 3.5
  - 3.6
  - 3.7
  - 3.8
sudo: false
install: pip install tox-travis
script: tox
matrix:
  include:
    - python: 3.6
      env: TOX_ENV=lint
      script: tox -e $TOX_ENV
    - python: 3.6
      env: TOX_ENV=docs
      script: tox -e $TOX_ENV
